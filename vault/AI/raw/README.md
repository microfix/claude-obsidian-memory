# Raw Inbox

Drop loose notes, outputs, clippings, and unstructured content here.

Run `/compile` to process everything into the correct files in the AI memory structure.

Processed files are moved to `.processed/` (created automatically).
